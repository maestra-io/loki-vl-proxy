version: "3.8"

# Fleet cache e2e test: 3 proxy instances with peer discovery + disk cache
# Usage:
#   docker compose up -d --build
#   go test -v -tags=e2e -timeout=120s ./test/e2e-fleet/

services:
  victorialogs:
    image: victoriametrics/victoria-logs:v1.52.0
    container_name: fleet-vl
    ports:
      - "9428:9428"
    command:
      - "-storageDataPath=/vlogs"
      - "-retentionPeriod=1d"
      - "-httpListenAddr=:9428"
    volumes:
      - vl-data:/vlogs
    healthcheck:
      # VictoriaLogs images are scratch-based from v1.52.0 (no shell, no wget);
      # the binary's -version exit path is the probe that works on every version.
      test: ["CMD", "/victoria-logs-prod", "-version"]
      interval: 3s
      timeout: 2s
      retries: 10
      start_period: 5s

  proxy-a:
    image: ${PROXY_IMAGE:-loki-vl-proxy:fleet-local}
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: fleet-proxy-a
    ports:
      - "3100:3100"
    command:
      - "-listen=:3100"
      - "-backend=http://victorialogs:9428"
      - "-log-level=debug"
      - "-cache-ttl=30s"
      - "-cache-max=1000"
      - "-disk-cache-path=/cache/proxy-a.db"
      - "-disk-cache-compress=true"
      - "-disk-cache-flush-size=10"
      - "-disk-cache-flush-interval=1s"
      - "-peer-self=proxy-a:3100"
      - "-peer-discovery=static"
      - "-peer-static=proxy-a:3100,proxy-b:3100,proxy-c:3100"
      - "-peer-auth-token=e2e-test-token"
      - "-server.admin-auth-token=e2e-test-token"
      - "-server.register-instrumentation=true"
    tmpfs:
      - /cache
    depends_on:
      victorialogs:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck"]
      interval: 3s
      timeout: 2s
      retries: 10
      start_period: 5s

  proxy-b:
    image: ${PROXY_IMAGE:-loki-vl-proxy:fleet-local}
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: fleet-proxy-b
    ports:
      - "3101:3100"
    command:
      - "-listen=:3100"
      - "-backend=http://victorialogs:9428"
      - "-log-level=debug"
      - "-cache-ttl=30s"
      - "-cache-max=1000"
      - "-disk-cache-path=/cache/proxy-b.db"
      - "-disk-cache-compress=true"
      - "-disk-cache-flush-size=10"
      - "-disk-cache-flush-interval=1s"
      - "-peer-self=proxy-b:3100"
      - "-peer-discovery=static"
      - "-peer-static=proxy-a:3100,proxy-b:3100,proxy-c:3100"
      - "-peer-auth-token=e2e-test-token"
      - "-server.admin-auth-token=e2e-test-token"
      - "-server.register-instrumentation=true"
    tmpfs:
      - /cache
    depends_on:
      victorialogs:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck"]
      interval: 3s
      timeout: 2s
      retries: 10
      start_period: 5s

  proxy-c:
    image: ${PROXY_IMAGE:-loki-vl-proxy:fleet-local}
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: fleet-proxy-c
    ports:
      - "3102:3100"
    command:
      - "-listen=:3100"
      - "-backend=http://victorialogs:9428"
      - "-log-level=debug"
      - "-cache-ttl=30s"
      - "-cache-max=1000"
      - "-disk-cache-path=/cache/proxy-c.db"
      - "-disk-cache-compress=true"
      - "-disk-cache-flush-size=10"
      - "-disk-cache-flush-interval=1s"
      - "-peer-self=proxy-c:3100"
      - "-peer-discovery=static"
      - "-peer-static=proxy-a:3100,proxy-b:3100,proxy-c:3100"
      - "-peer-auth-token=e2e-test-token"
      - "-server.admin-auth-token=e2e-test-token"
      - "-server.register-instrumentation=true"
    tmpfs:
      - /cache
    depends_on:
      victorialogs:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck"]
      interval: 3s
      timeout: 2s
      retries: 10
      start_period: 5s

volumes:
  vl-data:
